
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access on all documents to anyone
    // for prototyping. Make sure to restrict this for production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
